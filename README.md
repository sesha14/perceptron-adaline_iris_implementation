# perceptron-adaline_iris_implementation
The code includes implementation of perceptron and adaline models to 2,3 and 4 features
It includes data visualization and decision boundary visualization too.
